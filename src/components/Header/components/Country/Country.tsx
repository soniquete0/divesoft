import * as React from 'react';

const Country = () => (
  <div className={'header__select'}>
    <select>
      <option>Country</option>
      <option>CZ</option>
      <option>EN</option>
    </select>
  </div>
);

export default Country;